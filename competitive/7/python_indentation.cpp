#include <iostream>
#include <cmath>

int main() {
    const int MOD = (int)std::pow(10, 9) + 7;
    int N;
    std::cin >> N;

    char stmts[N + 1];
    for (int i = 1; i <= N; i++) {
        std::cin >> stmts[i];
    }
    
}
